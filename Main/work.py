from ast import match_case


# `match_case` is a function in Python's `ast` module that is used to match a specific case in a
# pattern matching expression. It is typically used in conjunction with the `match` statement
# introduced in Python 3.10 for structural pattern matching.
match_case